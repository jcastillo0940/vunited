<?php
namespace App\Http\Controllers; use App\Domain\Payments\Models\PaymentIntent; use App\Domain\Payments\Models\Refund; use App\Services\TilopayProvider; use Illuminate\Http\Request; use Illuminate\Support\Facades\DB; use Illuminate\Support\Str;
class InternalPaymentController extends Controller { public function create(Request $r){$d=$r->validate(['source'=>'required|in:store,ticketing','external_reference'=>'required|string|max:120','amount'=>'required|integer|min:1','currency'=>'required|string|size:3','metadata'=>'nullable|array']);$key=$r->header('Idempotency-Key');abort_unless($key,400,'Idempotency-Key requerido.');$old=DB::table('idempotency_keys')->where('key',$key)->first();if($old)return response()->json(json_decode($old->response,true));$p=PaymentIntent::create($d+['public_id'=>(string)Str::uuid(),'status'=>'created','correlation_id'=>$r->attributes->get('correlation_id')]);$provider=app(TilopayProvider::class);$result=$provider->create($d);$p->update(['provider'=>'tilopay','provider_reference'=>$result['reference'],'status'=>$result['status']]);$out=['id'=>$p->public_id,'status'=>$p->status,'provider_reference'=>$p->provider_reference,'checkout_url'=>$result['checkout_url']??null];DB::table('idempotency_keys')->insert(['key'=>$key,'operation'=>'payment.create','response'=>json_encode($out),'created_at'=>now(),'updated_at'=>now()]);return response()->json($out,201);}
 public function show(string $id){return PaymentIntent::where('public_id',$id)->firstOrFail();} public function cancel(string $id){$p=PaymentIntent::where('public_id',$id)->firstOrFail();abort_unless(in_array($p->status,['created','pending','requires_action']),409,'Estado no cancelable.');$p->update(['status'=>'cancelled']);return $p;} public function refund(Request $r,string $id){$p=PaymentIntent::where('public_id',$id)->firstOrFail();$d=$r->validate(['amount'=>'required|integer|min:1|max:'.$p->amount]);$key=$r->header('Idempotency-Key');abort_unless($key,400,'Idempotency-Key requerido.');$old=DB::table('idempotency_keys')->where('key',$key)->first();if($old)return response()->json(json_decode($old->response,true));$res=app(TilopayProvider::class)->refund($p->provider_reference,$d['amount'],$p->currency);$ref=Refund::create(['public_id'=>(string)Str::uuid(),'payment_intent_id'=>$p->id,'amount'=>$d['amount'],'currency'=>$p->currency,'status'=>$res['status'],'provider_reference'=>$p->provider_reference]);$out=['id'=>$ref->public_id,'status'=>$ref->status];DB::table('idempotency_keys')->insert(['key'=>$key,'operation'=>'refund.create','response'=>json_encode($out),'created_at'=>now(),'updated_at'=>now()]);return response()->json($out,201);} public function refundShow(string $id){return Refund::where('public_id',$id)->firstOrFail();} public function transactions(string $id){return DB::table('provider_transactions')->where('id',$id)->firstOrFail();} public function reconciliations(){return DB::table('reconciliations')->latest()->paginate(50);} }
