# ADR-004: APIs y eventos

- Estado: aceptada.
- Decisión: integración por APIs `/api/v1`, `/internal/v1` y `/webhooks/v1`, más
  eventos durables con outbox/inbox, correlación, reintentos e idempotencia.
